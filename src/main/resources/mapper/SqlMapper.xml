<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dmdb.mapper.SqlMapper">

    <update id="executeSql">${sql}</update>

    <select id="runQuery" resultType="java.util.LinkedHashMap">${sql}</select>

</mapper>